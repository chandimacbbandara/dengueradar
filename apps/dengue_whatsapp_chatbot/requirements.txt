Flask>=3.0,<4.0
flask-cors>=4.0,<7.0
python-dotenv>=1.0,<2.0
requests>=2.32,<3.0
# No separate LLM SDK is required; llm_service.py uses requests so provider
# integration stays isolated from the Flask and Vonage code.
